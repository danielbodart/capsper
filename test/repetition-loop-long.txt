Okay, very annoyingly, I've just lost some files that I created, so you are here to help me recreate those files as quickly as possible. So I've just copied over into this project a run TypeScript file and a bootstrap script from another project. And I want us to edit the run TypeScript file. So bootstrap will auto install Mize into a local directory and that's good, I want to use Mize. So once we've got bootstrap doing that, we should then get Mize to install, we need to install Bun and we need it to install Node. And the Node version should be specified in what is in the NVMRC file that's in here. And then, yeah, and so basically you should be able to do dot run on the TS file and it auto install everything that's needed to run this project. We then need to modify the run script and we need to have a setup action in there. The setup action needs to do everything that's described of needing to do inside the readme in this project. And we are going to find some stuff that isn't documented in that readme. And when that happens, we are going to just add it into the run script in the setup function and just slowly build it up. Now, ideally, I want the default target. The default target should call yarn build.